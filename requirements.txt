sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5). Pinned to 5.x: gradio 6.x pulls
# huggingface-hub>=1.0, which conflicts with sentence-transformers/transformers.
gradio==5.49.1
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
